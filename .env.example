# Copy this file to .env and fill in your values.

# InfluxDB admin password (set on first run)
INFLUX_ADMIN_PASSWORD=changeme

# InfluxDB API token (must match what InfluxDB is initialized with)
SF_INFLUX_TOKEN=changeme
